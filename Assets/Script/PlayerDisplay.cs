using UnityEngine;
using System.Collections;

public class PlayerDisplay : MonoBehaviour
{
    void Update()
    {
      /*  if (this.renderer.isVisible == true)
        {
            Debug.LogError("Tetst");
            this.transform.position = new Vector3(0, 0, 0);
        }
       * */
    }
}